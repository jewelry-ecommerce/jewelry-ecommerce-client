/** Graduation project: Grafana Faro is intentionally excluded. */
export function isFaroReady(): boolean {
  return false;
}

export async function ensureFaroInitializedAsync(): Promise<boolean> {
  return false;
}
