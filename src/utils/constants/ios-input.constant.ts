/** Min font-size (px) to prevent iOS Safari auto-zoom on input focus. */
export const IOS_INPUT_MIN_FONT_SIZE_PX = 16;

/** Touch-only media query — use with `@supports (-webkit-touch-callout: none)` for mobile iOS. */
export const IOS_INPUT_TOUCH_MEDIA = "(hover: none) and (pointer: coarse)";
