export enum FileStatus {
  USED = "Used",
  UNUSED = "Unused",
}

export enum FileType {
  IMAGE = "image",
  DOCUMENT = "document",
}

export enum FileTypeEnum {
  IMAGE = "image",
  DOCUMENT = "document",
  VIDEO = "video",
}

export enum MediaVisibility {
  PRIVATE = "Private",
  PUBLIC = "Public",
}
